# The demo DB is a build artifact, not source: `node demo/generate.mjs` (or
# `npm run sample`) rebuilds it in about 15 seconds, and Dockerfile.demo
# generates it during the image build. Keeping it out of git is what stops an
# 80MB binary from living in this repo's history forever.
data/
